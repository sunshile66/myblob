# 管理后台配置指引

## 📌 访问管理后台

访问地址：`http://127.0.0.1:8000/admin`

默认管理员账号：
- 用户名：`admin`
- 密码：`admin123456`

---

## 📧 第一步：配置邮箱（QQ邮箱）

### 1.1 准备工作 - 获取QQ邮箱授权码

1. 登录QQ邮箱：https://mail.qq.com/
2. 点击顶部「设置」→「账户」
3. 向下滚动到「POP3/IMAP/SMTP/Exchange/CardDAV/CalDAV服务」
4. 找到「POP3/SMTP服务」，点击「开启」
5. 按照提示发送短信验证
6. **验证成功后，复制显示的「授权码」**（重要！不是QQ密码！）

### 1.2 在管理后台配置

1. 登录管理后台
2. 在左侧菜单找到「Security」→「邮箱配置」
3. 点击「添加邮箱配置」
4. 填写信息：

| 字段 | 说明 | 示例值 |
|------|------|--------|
| 邮箱服务商 | 选择 `QQ邮箱` | QQ邮箱 |
| 邮箱账号 | 你的QQ邮箱 | `123456789@qq.com` |
| 密码/授权码 | 粘贴刚才获取的授权码 | `xxxxxxxxxxxx` |
| 发件人邮箱 | 同邮箱账号 | `123456789@qq.com` |
| 是否启用 | 勾选 ✅ | 是 |

5. 点击「保存」

### 1.3 测试邮箱配置

1. 在「邮箱配置」列表中，勾选刚才创建的配置
2. 在顶部「动作」下拉菜单中选择「发送测试邮件」
3. 点击「执行」
4. 查看你的QQ邮箱是否收到测试邮件

---

## 📱 第二步：配置短信（阿里云）

### 2.1 准备工作 - 阿里云短信配置

#### 2.1.1 开通短信服务

1. 登录阿里云：https://www.aliyun.com/
2. 搜索「短信服务」，点击进入
3. 点击「立即开通」（免费开通）

#### 2.1.2 创建签名

1. 在短信服务控制台，点击左侧「国内消息」→「签名管理」
2. 点击「添加签名」
3. 填写：
   - 签名名称：`MyBlob`（或你想要的名称）
   - 签名来源：选择「网站」
   - 网站名称：填写你的网站名称
   - 上传网站备案截图（如果有）
4. 点击「提交」，等待审核（通常1-2小时）

#### 2.1.3 创建模板

1. 点击左侧「模板管理」→「添加模板」
2. 填写：
   - 模板名称：`验证码`
   - 模板类型：选择「验证码」
   - 模板内容：`您的验证码是：${code}，请在5分钟内使用。`
   - 申请说明：`用于用户注册、登录验证`
3. 点击「提交」，等待审核

#### 2.1.4 获取AccessKey

1. 点击右上角头像 →「AccessKey管理」
2. 点击「创建AccessKey」
3. 验证手机号
4. **保存AccessKey ID和AccessKey Secret**（只显示一次！）

### 2.2 在管理后台配置

1. 回到Django管理后台
2. 在左侧菜单找到「Security」→「短信配置」
3. 点击「添加短信配置」
4. 填写信息：

| 字段 | 说明 | 示例值 |
|------|------|--------|
| 短信服务商 | 选择 `阿里云` | 阿里云 |
| AccessKey ID | 粘贴刚才获取的 | `LTAI5t...` |
| AccessKey Secret | 粘贴刚才获取的 | `abc123...` |
| 签名 | 阿里云审核通过的签名 | `MyBlob` |
| 模板CODE | 阿里云模板CODE | `SMS_123456789` |
| 是否启用 | 勾选 ✅ | 是 |

### 2.3 防盗刷配置（重要！）

在同一页面，设置发送限制：

| 字段 | 推荐值 | 说明 |
|------|--------|------|
| 每日发送限制 | 100 | 每天最多发送100条 |
| 每小时发送限制 | 20 | 每小时最多20条 |
| 每分钟发送限制 | 5 | 每分钟最多5条 |
| 同一IP限制 | 3 | 同一IP每天最多3条 |
| 同一接收者限制 | 2 | 同一手机号/邮箱每天最多2条 |

点击「保存」

---

## 🔐 第三步：配置安全参数

1. 在左侧菜单找到「Security」→「安全配置」
2. 点击「安全配置」（只有一条记录）
3. 根据需要调整：

### 验证码配置

| 配置项 | 说明 |
|--------|------|
| 启用验证码 | 勾选则需要验证码才能注册/登录 |
| 验证码长度 | 4-8位数字 |
| 验证码过期时间 | 单位秒，推荐300（5分钟） |

### 反爬虫配置

| 配置项 | 说明 |
|--------|------|
| 启用反爬虫 | 开启后会检测爬虫行为 |
| 每分钟请求限制 | 超过限制会被限流 |
| 每小时请求限制 | 超过限制会被限流 |

### 单点登录配置

| 配置项 | 说明 |
|--------|------|
| 启用单点登录 | 限制同一账号同时在线数量 |
| 最大同时在线数 | 1表示只能一个设备在线 |

### 流量监控配置

| 配置项 | 说明 |
|--------|------|
| 启用流量监控 | 记录所有请求 |
| 自动封禁 | 检测到异常自动封禁IP |
| 封禁时长 | 单位秒，推荐3600（1小时） |

---

## 📊 第四步：查看发送记录和统计

### 查看验证码发送记录

1. 在左侧菜单找到「Security」→「验证码记录」
2. 可以看到：
   - 接收者（手机号/邮箱）
   - 验证码
   - 类型（短信/邮箱）
   - 状态（成功/失败/待发送）
   - IP地址
   - 发送时间

### 筛选和搜索

- 按类型筛选：短信/邮箱
- 按状态筛选：成功/失败
- 按日期筛选：点击顶部日期选择器
- 搜索：输入手机号、邮箱或IP搜索

### 重新发送验证码

1. 在列表中勾选需要重新发送的记录
2. 在顶部「动作」下拉菜单选择「重新发送选中的验证码」
3. 点击「执行」

---

## ⚠️ 预警和防盗刷

### 系统会自动拦截的情况

| 情况 | 提示信息 |
|------|----------|
| 同一IP今日超过限制 | 同一IP今日已达到发送上限 X 次 |
| 同一接收者今日超过限制 | 同一接收者今日已达到发送上限 X 次 |
| 每分钟超过限制 | 每分钟已达到发送上限 X 次 |
| 每小时超过限制 | 每小时已达到发送上限 X 次 |
| 每日超过限制 | 今日已达到发送上限 X 次 |

### 查看预警

在「验证码记录」中，状态为「失败」的记录，错误信息列会显示具体原因。

---

## 🛠️ 常见问题

### Q: 邮箱发送失败怎么办？

A: 
1. 检查授权码是否正确（不是QQ密码！）
2. 确认QQ邮箱POP3/SMTP服务已开启
3. 在「邮箱配置」中测试发送
4. 查看Django终端错误日志

### Q: 短信发送失败怎么办？

A:
1. 确认阿里云签名和模板已审核通过
2. 检查AccessKey ID和Secret是否正确
3. 确认模板CODE正确
4. 查看「验证码记录」中的错误信息
5. 查看阿里云短信服务控制台的发送记录

### Q: 如何临时关闭验证码？

A: 在「安全配置」中，取消勾选「启用验证码」即可。

### Q: 如何查看被封禁的IP？

A: 在左侧菜单「Security」→「IP封禁」中查看。

### Q: 如何解除IP封禁？

A: 
1. 在「IP封禁」列表中勾选要解除的IP
2. 选择「解除选中的封禁」动作
3. 点击执行

---

## 📱 测试验证码功能

### 发送验证码API

```bash
POST /api/accounts/send-verification-code/
Content-Type: application/json

{
  "phone_or_email": "13800138000",
  "type": "sms"
}
```

或发送邮箱：

```bash
POST /api/accounts/send-verification-code/
Content-Type: application/json

{
  "phone_or_email": "test@example.com",
  "type": "email"
}
```

### 登录时使用验证码

```bash
POST /api/accounts/login/
Content-Type: application/json

{
  "username": "testuser",
  "password": "test123456",
  "phone_or_email": "13800138000",
  "code": "123456"
}
```

---

## ✅ 配置检查清单

- [ ] QQ邮箱授权码已获取
- [ ] 邮箱配置已添加并测试成功
- [ ] 阿里云短信签名已审核通过
- [ ] 阿里云短信模板已审核通过
- [ ] AccessKey已保存
- [ ] 短信配置已添加
- [ ] 防盗刷限制已设置
- [ ] 安全配置已根据需要调整
- [ ] 测试发送验证码成功
- [ ] 测试登录验证码验证成功

配置完成！现在你的网站已经拥有完整的验证码发送、防盗刷和统计功能了！
